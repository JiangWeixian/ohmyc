{
  "name": "@ohmyc/timeline-plugin",
  "version": "1.0.0",
  "type": "module",
  "files": [
    "dist",
    "hooks",
    "README.md"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build opencode.ts --outfile dist/index.js --target bun --external bun:sqlite --external @opencode-ai/plugin",
    "prepublishOnly": "bun run build",
    "test": "vitest run"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.14.31"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.14.31",
    "vitest": "^2.1.9"
  }
}
