---
phase: 10-config-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/SourceBadge.tsx
  - packages/ui/src/components/__tests__/SourceBadge.test.tsx
  - packages/shared/src/agentSchema.ts
  - packages/shared/src/skillSchema.ts
  - packages/shared/src/commandSchema.ts
autonomous: true
requirements:
  - FOUND-03
user_setup: []

must_haves:
  truths:
    - "SourceBadge component renders a visually distinct project variant with green color (#22c55e)"
    - "The project variant displays the label 'project' in uppercase"
    - "All three shared schemas (agent, skill, command) accept 'project' as a valid source value"
    - "The SourceBadge TypeScript type includes 'project' alongside existing variants"
  artifacts:
    - path: "packages/ui/src/components/SourceBadge.tsx"
      provides: "SourceBadge with 4 variants: local, profile, plugin, project"
      contains: "source === 'project'"
    - path: "packages/ui/src/components/__tests__/SourceBadge.test.tsx"
      provides: "Test coverage for project variant rendering"
      contains: "source=\"project\""
    - path: "packages/shared/src/agentSchema.ts"
      provides: "AgentSchema with source accepting 'project'"
      contains: "z.enum(['local', 'profile', 'plugin', 'project'])"
    - path: "packages/shared/src/skillSchema.ts"
      provides: "SkillSchema with source accepting 'project'"
      contains: "z.enum(['local', 'profile', 'plugin', 'project'])"
    - path: "packages/shared/src/commandSchema.ts"
      provides: "CommandSchema with source accepting 'project'"
      contains: "z.enum(['local', 'profile', 'plugin', 'project'])"
  key_links:
    - from: "packages/shared/src/agentSchema.ts"
      to: "packages/ui/src/components/SourceBadge.tsx"
      via: "TypeScript type inference through @claudeui/shared package"
      pattern: "source.*project"
    - from: "packages/ui/src/Explorer.tsx"
      to: "packages/ui/src/components/SourceBadge.tsx"
      via: "SourceBadge component rendering per entity.source"
      pattern: "SourceBadge source="
---

<objective>
Add a `project` variant to SourceBadge with green (#22c55e) styling and extend all three shared schemas (agent, skill, command) to accept `'project'` as a valid source value. This enables Phase 11 to tag project-scoped inventory items and have the UI render them with a distinct badge.

Purpose: UI can render project-scoped items with a visually distinct badge (D-06, D-07). API responses type-check correctly when carrying `source: 'project'` (preparation for Phase 11).

Output: Updated SourceBadge component, updated shared schemas, and verified test coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-config-foundation/10-CONTEXT.md
@.planning/phases/10-config-foundation/10-RESEARCH.md
@.planning/phases/10-config-foundation/10-UI-SPEC.md

<interfaces>
<!-- Current SourceBadge component (exact code to modify) -->
From packages/ui/src/components/SourceBadge.tsx:
```typescript
type InventorySource = 'local' | 'profile' | 'plugin';

interface SourceBadgeProps {
  source: InventorySource;
  pluginId?: string;
}

export function SourceBadge({ source, pluginId }: SourceBadgeProps) {
  if (source === 'local') {
    return (
      <span className="rounded-[var(--radius-sm)] bg-[var(--surface-overlay)] px-2 py-0.5 text-[10px] font-semibold uppercase text-[var(--text-tertiary)]">
        local
      </span>
    );
  }
  if (source === 'profile') {
    return (
      <span className="rounded-[var(--radius-sm)] bg-[var(--accent-blue)]/10 px-2 py-0.5 text-[10px] font-semibold uppercase text-[var(--accent-blue)]">
        profile
      </span>
    );
  }
  if (source === 'plugin') {
    const label = pluginId ? pluginId.split('@')[0] : 'plugin';
    return (
      <span className="inline-flex items-center gap-1 rounded-[var(--radius-sm)] bg-[var(--accent-purple)]/10 px-2 py-0.5 text-[10px] font-semibold uppercase text-[var(--accent-purple)]">
        <span>plugin</span>
        {pluginId ? <span className="normal-case">{label}</span> : null}
      </span>
    );
  }
  return null;
}
```

<!-- Current shared schema source fields (all three must add 'project') -->
From packages/shared/src/agentSchema.ts line 29:
```typescript
source: z.enum(['local', 'profile', 'plugin']),
```

From packages/shared/src/skillSchema.ts line 24:
```typescript
source: z.enum(['local', 'profile', 'plugin']),
```

From packages/shared/src/commandSchema.ts line 23:
```typescript
source: z.enum(['local', 'profile', 'plugin']),
```

<!-- Merge policy (D-08) documented in CONTEXT.md for Phase 11 reference -->
Merge policy: "Project items override global items when both provide the same component name. Both remain visible."
No separate policy document needed (D-09).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project variant to SourceBadge with tests</name>
  <files>
    packages/ui/src/components/SourceBadge.tsx
    packages/ui/src/components/__tests__/SourceBadge.test.tsx
  </files>
  <read_first>
    packages/ui/src/components/SourceBadge.tsx
    packages/ui/src/components/__tests__/SourceBadge.test.tsx
  </read_first>
  <behavior>
    - SourceBadge with source="project" renders a span with text "PROJECT"
    - SourceBadge with source="project" has className containing "#22c55e" for text color
    - SourceBadge with source="project" has className containing "#22c55e/10" for background
    - SourceBadge with source="project" has className containing "uppercase" and "font-semibold"
    - SourceBadge with source="project" does NOT render a pluginId sublabel
    - Existing variants (local, profile, plugin) continue to render correctly
    - InventorySource type includes 'project' alongside existing values
  </behavior>
  <action>
    **RED phase — write test first:**

    Open `packages/ui/src/components/__tests__/SourceBadge.test.tsx` and add a test for the project variant after the existing 3 tests:

    ```typescript
    it('renders a project source label with green styling', () => {
      renderWithProviders(<SourceBadge source="project" />);

      const badge = screen.getByText('project');
      expect(badge).toBeInTheDocument();
      // D-06: green color #22c55e
      expect(badge.className).toContain('#22c55e/10');
      expect(badge.className).toContain('text-[#22c55e]');
      // D-07: uppercase label (applied via Tailwind class)
      expect(badge.className).toContain('uppercase');
    });

    it('does not render pluginId sub-label for project variant', () => {
      renderWithProviders(<SourceBadge source="project" />);

      // project variant should not show any secondary label
      expect(screen.queryByText(/@/)).not.toBeInTheDocument();
    });
    ```

    Run `cd packages/ui && npx vitest run -- SourceBadge` — tests must FAIL because `project` is not yet a valid source value and the variant doesn't exist. Commit: `test(10-02): add failing tests for SourceBadge project variant`.

    **GREEN phase — implement the project variant:**

    Open `packages/ui/src/components/SourceBadge.tsx` and make these changes:

    1. Update the InventorySource type:
       ```typescript
       type InventorySource = 'local' | 'profile' | 'plugin' | 'project';
       ```

    2. Insert the project variant block **before** the `return null` statement, following the exact same pattern as the profile variant (no pluginId sub-label, single span):
       ```typescript
       if (source === 'project') {
         return (
           <span className="rounded-[var(--radius-sm)] bg-[#22c55e]/10 px-2 py-0.5 text-[10px] font-semibold uppercase text-[#22c55e]">
             project
           </span>
         );
       }
       ```

    3. The complete source order should be: local → profile → plugin → project → return null

    Run `cd packages/ui && npx vitest run -- SourceBadge` — all 5 tests must pass (3 existing + 2 new). Commit: `feat(10-02): add project variant to SourceBadge`.
  </action>
  <verify>
    <automated>cd packages/ui && npx vitest run -- SourceBadge --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SourceBadge.tsx InventorySource type includes 'project'
    - SourceBadge renders project variant with green color (#22c55e) and /10 opacity background
    - Project variant uses "project" label with uppercase styling (D-07)
    - Project variant has no pluginId sub-label (matches local/profile pattern)
    - All 5 SourceBadge tests pass: local, profile, plugin (with pluginId), project (styling), project (no sub-label)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 'project' to shared schema source enums and verify type flow</name>
  <files>
    packages/shared/src/agentSchema.ts
    packages/shared/src/skillSchema.ts
    packages/shared/src/commandSchema.ts
  </files>
  <read_first>
    packages/shared/src/agentSchema.ts
    packages/shared/src/skillSchema.ts
    packages/shared/src/commandSchema.ts
    packages/shared/src/index.ts
  </read_first>
  <acceptance_criteria>
    - packages/shared/src/agentSchema.ts contains `z.enum(['local', 'profile', 'plugin', 'project'])` on the source field
    - packages/shared/src/skillSchema.ts contains `z.enum(['local', 'profile', 'plugin', 'project'])` on the source field
    - packages/shared/src/commandSchema.ts contains `z.enum(['local', 'profile', 'plugin', 'project'])` on the source field
    - `cd packages/shared && npx tsc --noEmit` exits 0 (type check passes)
    - `cd packages/cli && npx vitest run` exits 0 (existing tests pass with updated schemas)
    - `cd packages/ui && npx vitest run` exits 0 (UI tests pass)
  </acceptance_criteria>
  <action>
    **Step 1: Update agentSchema.ts**

    Open `packages/shared/src/agentSchema.ts`. On line 29, change:
    ```typescript
    source: z.enum(['local', 'profile', 'plugin']),
    ```
    to:
    ```typescript
    source: z.enum(['local', 'profile', 'plugin', 'project']),
    ```

    **Step 2: Update skillSchema.ts**

    Open `packages/shared/src/skillSchema.ts`. On line 24, change:
    ```typescript
    source: z.enum(['local', 'profile', 'plugin']),
    ```
    to:
    ```typescript
    source: z.enum(['local', 'profile', 'plugin', 'project']),
    ```

    **Step 3: Update commandSchema.ts**

    Open `packages/shared/src/commandSchema.ts`. On line 23, change:
    ```typescript
    source: z.enum(['local', 'profile', 'plugin']),
    ```
    to:
    ```typescript
    source: z.enum(['local', 'profile', 'plugin', 'project']),
    ```

    **Step 4: Verify type flow across packages**

    Run the TypeScript compiler on the shared package to confirm type exports are valid:
    ```bash
    cd packages/shared && npx tsc --noEmit
    ```

    Run the CLI test suite to confirm no regressions:
    ```bash
    cd packages/cli && npx vitest run
    ```

    Run the UI test suite to confirm SourceBadge still works:
    ```bash
    cd packages/ui && npx vitest run
    ```

    **Step 5: Verify merge policy documentation**

    The merge policy is already documented in CONTEXT.md at decision D-08: "Project items override global items when both provide the same component name. Both remain visible." Per D-09, no separate policy document is needed. Verify this text exists in `10-CONTEXT.md` (it should already be there from the discussion phase).

    Commit: `feat(10-02): add 'project' to shared schema source enums`
  </action>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit 2>&1 && cd ../.. && cd packages/cli && npx vitest run 2>&1 | tail -10 && cd ../.. && cd packages/ui && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All three shared schemas (agent, skill, command) accept 'project' as a valid source value
    - TypeScript compilation passes with no errors
    - CLI test suite green (schemas backwards compatible — 'project' is additive)
    - UI test suite green (SourceBadge renders all 4 variants)
    - FOUND-03 requirement satisfied: SourceBadge renders project variant AND schemas support project source
    - Merge policy documented in CONTEXT.md per D-08/D-09
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → UI | Source field values now include 'project' — data must be validated by schema before UI rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Spoofing | Source field injection | mitigate | z.enum validation in shared schemas rejects any value that is not 'local', 'profile', 'plugin', or 'project'. Invalid source values are rejected at parse time. |
| T-10-05 | Information Disclosure | Project badge reveals project path existence | accept | Badge only shows "PROJECT" label with no path information. Per D-07, no project folder name is displayed. |

</threat_model>

<verification>
1. SourceBadge renders project variant: `cd packages/ui && npx vitest run -- SourceBadge`
2. Shared schemas compile: `cd packages/shared && npx tsc --noEmit`
3. CLI tests pass: `cd packages/cli && npx vitest run`
4. UI tests pass: `cd packages/ui && npx vitest run`
5. Grep check: all three schema files have 'project' in their source enum: `grep -n "project" packages/shared/src/agentSchema.ts packages/shared/src/skillSchema.ts packages/shared/src/commandSchema.ts`
</verification>

<success_criteria>
- SourceBadge renders `project` variant with green (#22c55e) color per D-06
- SourceBadge displays "project" label in uppercase per D-07
- All three shared schemas include 'project' in source z.enum
- TypeScript compilation passes across all packages
- All existing tests continue to pass (backward compatible change)
- Merge policy (D-08) documented in CONTEXT.md for Phase 11 reference
</success_criteria>

<output>
After completion, create `.planning/phases/10-config-foundation/10-02-SUMMARY.md`
</output>