---
phase: 01-store-and-inventory-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-00
  - 01-01
files_modified:
  - packages/ui/src/ProfilesView.tsx
  - packages/ui/src/components/profiles/ProfilesSidebar.tsx
  - packages/ui/src/hooks/useStore.ts
  - packages/ui/src/components/store/StoreComponentList.tsx
  - packages/ui/src/components/store/StoreComponentEditor.tsx
  - packages/ui/src/components/store/DeleteConfirmDialog.tsx
  - packages/ui/src/components/store/ImportComponentsDialog.tsx
  - packages/ui/src/components/store/__tests__/ImportComponentsDialog.test.tsx
autonomous: true
requirements:
  - STORE-01
  - STORE-02
  - STORE-03
  - STORE-04
  - STORE-05
  - STORE-06
must_haves:
  truths:
    - "User can browse store-managed agents, skills, and commands from one store view using name search and a type filter."
    - "User can create, edit, and delete store-managed components from the store UI without touching AGENT_HOME inventory views."
    - "User can preview import conflicts, cancel safely, or confirm a single Overwrite All action from the store UI."
  artifacts:
    - path: "packages/ui/src/components/store/StoreComponentList.tsx"
      provides: "Unified store browse/search/filter view with provenance and reference counts"
    - path: "packages/ui/src/components/store/ImportComponentsDialog.tsx"
      provides: "Two-step import preview/apply flow"
    - path: "packages/ui/src/hooks/useStore.ts"
      provides: "Client hooks for provenance-aware store lists and import preview/apply"
  key_links:
    - from: "packages/ui/src/components/store/ImportComponentsDialog.tsx"
      to: "/api/store/import"
      via: "dryRun preview then confirmed overwrite apply"
      pattern: "sourceDir.*dryRun|overwrite"
    - from: "packages/ui/src/components/store/StoreComponentList.tsx"
      to: "packages/ui/src/hooks/useStore.ts"
      via: "combined store queries and delete/import mutations"
      pattern: "useStore(Agents|Skills|Commands|Import)"
    - from: "packages/ui/src/ProfilesView.tsx"
      to: "packages/ui/src/components/store/StoreComponentList.tsx"
      via: "store route selection and initial type filter"
      pattern: "StoreComponentList"
---

<objective>
Finish the Phase 1 store experience by wiring the new store API into the UI’s browse, import, and CRUD flows.

Purpose: Put the canonical store model in front of the user with the minimal, power-user-oriented UX the phase requires.
Output: A unified store browser with search and type filtering, provenance details, and an import dialog that previews conflicts before apply.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-store-and-inventory-foundation/01-CONTEXT.md
@.planning/phases/01-store-and-inventory-foundation/01-RESEARCH.md
@.planning/phases/01-store-and-inventory-foundation/01-UI-SPEC.md
@.planning/phases/01-store-and-inventory-foundation/01-00-PLAN.md
@.planning/phases/01-store-and-inventory-foundation/01-01-PLAN.md
@packages/ui/src/ProfilesView.tsx
@packages/ui/src/components/profiles/ProfilesSidebar.tsx
@packages/ui/src/hooks/useStore.ts
@packages/ui/src/components/store/StoreComponentList.tsx
@packages/ui/src/components/store/StoreComponentEditor.tsx
@packages/ui/src/components/store/DeleteConfirmDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the store view into a searchable, type-filtered canonical browser</name>
  <read_first>
    packages/ui/src/ProfilesView.tsx
    packages/ui/src/components/profiles/ProfilesSidebar.tsx
    packages/ui/src/hooks/useStore.ts
    packages/ui/src/components/store/StoreComponentList.tsx
    .planning/phases/01-store-and-inventory-foundation/01-01-PLAN.md
    .planning/phases/01-store-and-inventory-foundation/01-UI-SPEC.md
  </read_first>
  <files>
    packages/ui/src/ProfilesView.tsx
    packages/ui/src/components/profiles/ProfilesSidebar.tsx
    packages/ui/src/hooks/useStore.ts
    packages/ui/src/components/store/StoreComponentList.tsx
  </files>
  <action>Refactor the store area into a unified browser instead of three isolated list screens. Support the exact type filter values `all`, `agents`, `skills`, and `commands`; keep the control in the store list header area, and keep in-place name search in the same header. `ProfilesView` should accept `/profiles/store` as the default store route and may still accept `/profiles/store/:category` as an initial filter. `StoreComponentList` must fetch all three store lists, flatten them into rows with the exact primary fields `type`, `name`, `description`, and `profile reference count`, and then apply local filtering by search text plus type filter without causing route changes. Provenance must stay secondary: render `importPath` and `importedAt` in an expandable metadata block, tooltip, or hover card rather than as a dominant main-row column. Keep the empty state copy exactly `No components in this store yet` and `Import agents, skills, or commands from an existing Claude-compatible directory to start building a canonical local store.`</action>
  <acceptance_criteria>
    `packages/ui/src/components/store/StoreComponentList.tsx` contains the literal filter values `all`, `agents`, `skills`, and `commands`.
    `packages/ui/src/components/store/StoreComponentList.tsx` contains the exact empty-state heading `No components in this store yet`.
    `packages/ui/src/components/store/StoreComponentList.tsx` contains the fields `type`, `name`, `description`, and reference-count rendering for each row.
    `packages/ui/src/components/store/StoreComponentList.tsx` contains the strings `importPath` and `importedAt`.
    `packages/ui/src/ProfilesView.tsx` handles `/profiles/store` as a valid store selection.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @claudeui/ui test -- src/components/store/__tests__/StoreComponentList.test.tsx</automated>
  </verify>
  <done>The store UI is a single browse surface with name search and type filtering, and it exposes provenance details without cluttering the main list rows.</done>
</task>

<task type="auto">
  <name>Task 2: Wire create/edit/delete and import-confirmation flows to the canonical store API</name>
  <read_first>
    packages/ui/src/hooks/useStore.ts
    packages/ui/src/components/store/StoreComponentList.tsx
    packages/ui/src/components/store/StoreComponentEditor.tsx
    packages/ui/src/components/store/DeleteConfirmDialog.tsx
    packages/cli/src/server/routes/store.ts
    .planning/phases/01-store-and-inventory-foundation/01-01-PLAN.md
    .planning/phases/01-store-and-inventory-foundation/01-UI-SPEC.md
  </read_first>
  <files>
    packages/ui/src/hooks/useStore.ts
    packages/ui/src/components/store/StoreComponentList.tsx
    packages/ui/src/components/store/StoreComponentEditor.tsx
    packages/ui/src/components/store/DeleteConfirmDialog.tsx
    packages/ui/src/components/store/ImportComponentsDialog.tsx
    packages/ui/src/components/store/__tests__/ImportComponentsDialog.test.tsx
  </files>
  <action>Extend `useStoreImport()` so the UI can call the backend in two exact phases: preview with `{ sourceDir, dryRun: true, overwrite: false }`, then apply with `{ sourceDir, dryRun: false, overwrite: false }` or `{ sourceDir, dryRun: false, overwrite: true }`. Create `ImportComponentsDialog.tsx` with the exact primary CTA label `Import Components`, destructive overwrite title `Overwrite existing components?`, and an explicit `Overwrite All` action for collision lists. The dialog must list every conflict returned by the backend before any overwrite occurs. Update `packages/ui/src/components/store/__tests__/ImportComponentsDialog.test.tsx` so the overwrite-confirmation flow is automated end to end at the component level: preview renders the returned conflicts, `Cancel` closes without issuing an overwrite apply call, and `Overwrite All` issues the second mutation with `dryRun: false` and `overwrite: true`. Keep store CRUD in the store surface only: `StoreComponentEditor` continues to create and edit store-managed components, `DeleteConfirmDialog` uses the Phase 1 copy contract `Delete this store component?` and `This component is still referenced by profiles. Delete anyway?`, and no AGENT_HOME inventory screen gains edit actions. On successful import, invalidate the `['store']` query family so the unified browser refreshes immediately.</action>
  <acceptance_criteria>
    `packages/ui/src/hooks/useStore.ts` contains both `dryRun` and `overwrite` in the import mutation payloads.
    `packages/ui/src/components/store/ImportComponentsDialog.tsx` contains the exact strings `Import Components`, `Overwrite existing components?`, and `Overwrite All`.
    `packages/ui/src/components/store/__tests__/ImportComponentsDialog.test.tsx` contains explicit assertions for preview, cancel, and overwrite-all mutation paths.
    `packages/ui/src/components/store/DeleteConfirmDialog.tsx` contains the exact strings `Delete this store component?` and `Delete anyway?`.
    `packages/ui/src/components/store/StoreComponentList.tsx` opens the import dialog and refreshes store queries after import success.
    No inventory file such as `packages/ui/src/Explorer.tsx` is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @claudeui/ui test -- src/components/store/__tests__/StoreComponentEditor.test.tsx src/components/store/__tests__/StoreComponentList.test.tsx src/components/store/__tests__/ImportComponentsDialog.test.tsx && pnpm --filter @claudeui/cli test -- src/server/routes/__tests__/store.test.ts</automated>
  </verify>
  <done>The UI can create, edit, delete, and import store-managed components against the new backend contract, including a required conflict preview and a one-click overwrite-all confirmation path.</done>
</task>

</tasks>

<verification>
Run the named store UI behavior suites plus the store route tests so search/filtering, provenance disclosure, store CRUD, and overwrite confirmation are all covered automatically. Keep the remaining manual check limited to visual hierarchy and the absence of edit controls in Explorer.
</verification>

<success_criteria>
Users can browse the canonical store from one minimal screen, filter it by name and type, inspect lightweight provenance, and complete all required store CRUD and import flows without crossing the Phase 1 boundary into AGENT_HOME editing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-store-and-inventory-foundation/01-03-SUMMARY.md`
</output>
