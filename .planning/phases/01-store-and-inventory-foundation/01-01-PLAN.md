---
phase: 01-store-and-inventory-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/storeSchema.ts
  - packages/shared/src/index.ts
  - packages/shared/src/agentSchema.ts
  - packages/shared/src/skillSchema.ts
  - packages/shared/src/commandSchema.ts
  - packages/cli/src/server/services/storeService.ts
  - packages/cli/src/server/routes/store.ts
  - packages/cli/src/server/services/__tests__/storeService.test.ts
  - packages/cli/src/server/routes/__tests__/store.test.ts
autonomous: true
requirements:
  - STORE-01
  - STORE-02
  - STORE-03
  - STORE-06
must_haves:
  truths:
    - "User can scan a Claude-compatible directory and see every conflicting store component before any overwrite happens."
    - "User can confirm a second import call that overwrites the scanned conflicts with one apply action."
    - "Imported store items retain importPath and importedAt metadata after page reload."
  artifacts:
    - path: "packages/shared/src/storeSchema.ts"
      provides: "Typed store import and provenance contracts"
    - path: "packages/cli/src/server/services/storeService.ts"
      provides: "Dry-run/apply import workflow, recursive skill copy, provenance persistence"
    - path: "packages/cli/src/server/routes/store.ts"
      provides: "Validated /api/store/import contract and provenance-aware store responses"
  key_links:
    - from: "packages/cli/src/server/routes/store.ts"
      to: "packages/cli/src/server/services/storeService.ts"
      via: "POST /api/store/import using dryRun and overwrite flags"
      pattern: "storeService\\.(scanImport|applyImport|import)"
    - from: "packages/cli/src/server/services/storeService.ts"
      to: "packages/shared/src/storeSchema.ts"
      via: "StoreImportResult and provenance types"
      pattern: "StoreImport(Result|Conflict|Provenance)"
    - from: "packages/cli/src/server/services/storeService.ts"
      to: "store/.metadata/imports.json"
      via: "persistent provenance index"
      pattern: "\\.metadata/imports\\.json"
---

<objective>
Establish the canonical store contract for Phase 1 by hardening import behavior and persisting lightweight provenance.

Purpose: Make the store trustworthy before UI work depends on it.
Output: Typed import/provenance schemas, a two-step import flow with overwrite support, recursive skill import, and backend tests covering the new behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-store-and-inventory-foundation/01-CONTEXT.md
@.planning/phases/01-store-and-inventory-foundation/01-RESEARCH.md
@docs/superpowers/specs/2026-03-25-profiler-design.md
@packages/cli/src/server/services/storeService.ts
@packages/cli/src/server/routes/store.ts
@packages/cli/src/server/services/__tests__/storeService.test.ts
@packages/cli/src/server/routes/__tests__/store.test.ts
@packages/shared/src/agentSchema.ts
@packages/shared/src/skillSchema.ts
@packages/shared/src/commandSchema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the store import and provenance contract</name>
  <read_first>
    packages/shared/src/agentSchema.ts
    packages/shared/src/skillSchema.ts
    packages/shared/src/commandSchema.ts
    packages/shared/src/index.ts
    packages/cli/src/server/services/storeService.ts
  </read_first>
  <files>
    packages/shared/src/storeSchema.ts
    packages/shared/src/index.ts
    packages/shared/src/agentSchema.ts
    packages/shared/src/skillSchema.ts
    packages/shared/src/commandSchema.ts
    packages/cli/src/server/services/storeService.ts
    packages/cli/src/server/services/__tests__/storeService.test.ts
  </files>
  <action>Create `packages/shared/src/storeSchema.ts` and export these exact Zod contracts plus inferred types: `StoreComponentTypeSchema = z.enum(['agents','skills','commands'])`, `StoreComponentProvenanceSchema = z.object({ importPath: z.string(), importedAt: z.string() })`, `StoreImportConflictSchema = z.object({ type: StoreComponentTypeSchema, id: z.string(), sourcePath: z.string(), destinationPath: z.string() })`, `StoreImportRequestSchema = z.object({ sourceDir: z.string(), dryRun: z.boolean().optional(), overwrite: z.boolean().optional() })`, and `StoreImportResultSchema = z.object({ imported: z.number(), skipped: z.number(), overwritten: z.number(), errors: z.array(z.string()), conflicts: z.array(StoreImportConflictSchema) })`. Re-export the new module from `packages/shared/src/index.ts`. Extend `AgentSchema`, `SkillSchema`, and `CommandSchema` with `provenance: StoreComponentProvenanceSchema.optional()` so store list/detail responses can carry import metadata without changing the core entity shape. In `StoreService`, persist provenance in the exact file `path.join(storeDir, '.metadata', 'imports.json')` using an object with top-level keys `agents`, `skills`, and `commands`. Replace the single `import(sourceDir)` implementation with a two-step service boundary that can scan conflicts before writing and can apply imports with overwrite enabled; recursive skill copying must include nested directories and assets below each skill folder, not only top-level files. Add service tests for: dry-run reporting conflicts without copying files, overwrite updating an existing agent while incrementing `overwritten`, nested skill assets being copied, and provenance entries containing both `importPath` and ISO `importedAt` strings.</action>
  <acceptance_criteria>
    `packages/shared/src/storeSchema.ts` contains the exact exported schema names `StoreImportRequestSchema`, `StoreImportResultSchema`, and `StoreComponentProvenanceSchema`.
    `packages/shared/src/agentSchema.ts`, `packages/shared/src/skillSchema.ts`, and `packages/shared/src/commandSchema.ts` each contain the string `provenance: StoreComponentProvenanceSchema.optional()`.
    `packages/cli/src/server/services/storeService.ts` contains the literal path fragment `.metadata', 'imports.json'`.
    `packages/cli/src/server/services/storeService.ts` contains logic that uses both `dryRun` and `overwrite`.
    `packages/cli/src/server/services/__tests__/storeService.test.ts` contains tests covering `nested skill assets`, `dry-run`, `overwrite`, and `importPath`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @claudeui/cli test -- src/server/services/__tests__/storeService.test.ts</automated>
  </verify>
  <done>The shared store contract exists, recursive import behavior is implemented behind the service boundary, provenance is persisted outside imported Claude files, and the service test suite covers conflict preview, overwrite, nested skills, and metadata persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the new import flow and provenance through store routes</name>
  <read_first>
    packages/cli/src/server/routes/store.ts
    packages/cli/src/server/services/storeService.ts
    packages/shared/src/storeSchema.ts
    packages/cli/src/server/routes/__tests__/store.test.ts
  </read_first>
  <files>
    packages/cli/src/server/routes/store.ts
    packages/cli/src/server/routes/__tests__/store.test.ts
    packages/cli/src/server/services/storeService.ts
  </files>
  <action>Update `POST /api/store/import` to parse the exact body shape `{ sourceDir: string, dryRun?: boolean, overwrite?: boolean }` with `StoreImportRequestSchema`. The route must return the exact result shape `{ imported, skipped, overwritten, errors, conflicts }` from the service, and it must reject `overwrite: true` when `dryRun: true`. When `dryRun` is true, the route must not write files; when `dryRun` is false and `overwrite` is false, collisions must remain listed in `conflicts` and no destination file may be replaced. Update the store list/detail handlers so `GET /api/store/agents`, `GET /api/store/skills`, `GET /api/store/commands`, and their `/:name` variants include `provenance` when metadata exists. Extend route tests to assert the dry-run response includes a typed `conflicts` array, overwrite apply increments `overwritten`, and store detail/list responses include `provenance.importPath` after an import.</action>
  <acceptance_criteria>
    `packages/cli/src/server/routes/store.ts` contains the strings `dryRun`, `overwrite`, and `StoreImportRequestSchema`.
    `packages/cli/src/server/routes/store.ts` returns the keys `imported`, `skipped`, `overwritten`, `errors`, and `conflicts` from the import handler.
    `packages/cli/src/server/routes/store.ts` maps stored provenance onto list and detail responses for agents, skills, and commands.
    `packages/cli/src/server/routes/__tests__/store.test.ts` contains assertions for `conflicts`, `overwritten`, and `provenance`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @claudeui/cli test -- src/server/routes/__tests__/store.test.ts src/server/services/__tests__/storeService.test.ts</automated>
  </verify>
  <done>The store API now supports preview-before-apply imports, explicit overwrite confirmation, and provenance-aware store responses for all three store component types.</done>
</task>

</tasks>

<verification>
Run the targeted CLI tests for store service and store routes. Confirm the generated import result includes conflicts and overwritten counts, and confirm the provenance metadata survives a second GET after import.
</verification>

<success_criteria>
Phase 1 store imports can be previewed before overwriting, skill imports preserve nested assets, and every imported store component can surface import path and import time through the API without mutating the imported Claude asset files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-store-and-inventory-foundation/01-01-SUMMARY.md`
</output>
