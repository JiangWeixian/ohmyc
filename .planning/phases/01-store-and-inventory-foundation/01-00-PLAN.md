---
phase: 01-store-and-inventory-foundation
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/package.json
  - packages/ui/vitest.config.ts
  - packages/ui/src/test/setup.ts
  - packages/ui/src/test/renderWithProviders.tsx
  - packages/ui/src/test/renderWithProviders.smoke.test.tsx
  - packages/ui/src/components/__tests__/SourceBadge.test.tsx
  - packages/ui/src/__tests__/Explorer.inventory.test.tsx
  - packages/ui/src/components/store/__tests__/StoreComponentList.test.tsx
  - packages/ui/src/components/store/__tests__/StoreComponentEditor.test.tsx
  - packages/ui/src/components/store/__tests__/ImportComponentsDialog.test.tsx
autonomous: true
requirements:
  - INV-01
  - INV-02
  - STORE-04
  - STORE-05
  - STORE-06
must_haves:
  truths:
    - "Phase 1 UI behaviors can be verified by repeatable automated tests before the UI implementation plans run."
    - "Inventory source labeling and Explorer environment inspection have named UI tests that can fail on regressions."
    - "Store browsing, provenance disclosure, store CRUD, and overwrite-confirmation flows have named UI tests that later plans can use as their primary verification commands."
  artifacts:
    - path: "packages/ui/vitest.config.ts"
      provides: "Vitest jsdom runner for the UI package"
    - path: "packages/ui/src/test/renderWithProviders.tsx"
      provides: "Shared React Query and Router-aware test render helper"
    - path: "packages/ui/src/test/renderWithProviders.smoke.test.tsx"
      provides: "Package-local smoke test proving jsdom, setup loading, and renderWithProviders work together"
    - path: "packages/ui/src/components/__tests__/SourceBadge.test.tsx"
      provides: "INV-01 badge rendering coverage"
    - path: "packages/ui/src/__tests__/Explorer.inventory.test.tsx"
      provides: "INV-02 Explorer plugin and config coverage"
    - path: "packages/ui/src/components/store/__tests__/StoreComponentList.test.tsx"
      provides: "STORE-04 and STORE-06 store browser coverage"
    - path: "packages/ui/src/components/store/__tests__/StoreComponentEditor.test.tsx"
      provides: "STORE-05 store editor and delete flow coverage"
    - path: "packages/ui/src/components/store/__tests__/ImportComponentsDialog.test.tsx"
      provides: "STORE-01/02/03 overwrite preview, cancel, and Overwrite All coverage"
  key_links:
    - from: "packages/ui/package.json"
      to: "packages/ui/vitest.config.ts"
      via: "UI test script invoking the package-local Vitest runner"
      pattern: "\"test\": \"vitest run\""
    - from: "packages/ui/src/test/renderWithProviders.tsx"
      to: "packages/ui/src/components/store/__tests__/StoreComponentList.test.tsx"
      via: "shared provider wrapper for routed/query-backed store UI"
      pattern: "renderWithProviders"
    - from: "packages/ui/src/test/setup.ts"
      to: "packages/ui/src/__tests__/Explorer.inventory.test.tsx"
      via: "jest-dom and DOM cleanup setup"
      pattern: "@testing-library/jest-dom"
---

<objective>
Create the Wave 0 UI test harness and baseline behavior suites required by the Phase 1 validation contract.

Purpose: Give the later UI plans concrete, automated behavior checks instead of relying on build-only verification.
Output: A `packages/ui` Vitest setup plus named UI test files for source badges, Explorer inventory panels, store browsing, and store CRUD flows.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-store-and-inventory-foundation/01-CONTEXT.md
@.planning/phases/01-store-and-inventory-foundation/01-VALIDATION.md
@packages/ui/package.json
@packages/ui/vite.config.ts
@packages/ui/src/components/SourceBadge.tsx
@packages/ui/src/Explorer.tsx
@packages/ui/src/components/store/StoreComponentList.tsx
@packages/ui/src/components/store/StoreComponentEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a package-local UI test runner and shared render utilities</name>
  <files>
    packages/ui/package.json
    packages/ui/vitest.config.ts
    packages/ui/src/test/setup.ts
    packages/ui/src/test/renderWithProviders.tsx
    packages/ui/src/test/renderWithProviders.smoke.test.tsx
  </files>
  <action>Extend `packages/ui/package.json` with a `test` script that runs `vitest run`, and add the minimum `devDependencies` needed for React component tests in jsdom: `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, and `@testing-library/user-event`. Create `packages/ui/vitest.config.ts` as a package-local config that mirrors the existing Vite React setup but sets `test.environment = 'jsdom'`, loads `src/test/setup.ts`, and allows path resolution from the UI package root. Create `packages/ui/src/test/setup.ts` to register `@testing-library/jest-dom`, clear mocks, and clean up DOM state after each test. Create `packages/ui/src/test/renderWithProviders.tsx` with a shared helper that wraps rendered components in a fresh `QueryClientProvider` and `MemoryRouter` so routed, React Query-driven UI components can be tested without per-file boilerplate. Add `packages/ui/src/test/renderWithProviders.smoke.test.tsx` as a real UI smoke test that renders a trivial component through `renderWithProviders`, asserts a jsdom matcher such as `toBeInTheDocument()`, and proves `src/test/setup.ts` plus the shared wrapper are actually executed by the package-local test runner.</action>
  <verify>
    <automated>pnpm --filter @claudeui/ui test -- src/test/renderWithProviders.smoke.test.tsx</automated>
  </verify>
  <done>The UI package exposes a working `pnpm --filter @claudeui/ui test` command, Vitest runs a real jsdom test with shared setup loaded, and later Phase 1 UI tests have a provider-aware render helper they can depend on.</done>
</task>

<task type="auto">
  <name>Task 2: Create the required Phase 1 UI behavior test suites</name>
  <files>
    packages/ui/src/components/__tests__/SourceBadge.test.tsx
    packages/ui/src/__tests__/Explorer.inventory.test.tsx
    packages/ui/src/components/store/__tests__/StoreComponentList.test.tsx
    packages/ui/src/components/store/__tests__/StoreComponentEditor.test.tsx
    packages/ui/src/components/store/__tests__/ImportComponentsDialog.test.tsx
    packages/ui/src/test/renderWithProviders.tsx
  </files>
  <action>Create the exact test files required by `01-VALIDATION.md` and make them meaningful, not placeholders. `SourceBadge.test.tsx` must assert visible output for `local`, `profile`, and `plugin` sources, including plugin rows that still identify the item as plugin-provided. `Explorer.inventory.test.tsx` must cover the read-only plugin/environment UI contract by asserting the `Current environment` summary, enabled plugin state, bundled component counts, and hook/MCP/LSP count rendering using mocked hooks. `StoreComponentList.test.tsx` must cover the unified store browser contract: local name search, `all|agents|skills|commands` type filtering, the exact empty-state copy, and provenance fields that are present but secondary. `StoreComponentEditor.test.tsx` must cover the store-only create/edit/delete UI contract by asserting validation messages, save callbacks for create and edit flows, and the destructive-copy states later required by `DeleteConfirmDialog` usage. Add `ImportComponentsDialog.test.tsx` to cover the overwrite path explicitly: previewing returned conflicts, canceling back out without applying, and issuing a final `Overwrite All` action against a mocked `useStoreImport()` mutation. Keep the tests focused on Phase 1 requirements and use mocked hooks instead of network calls.</action>
  <verify>
    <automated>pnpm --filter @claudeui/ui test -- src/components/__tests__/SourceBadge.test.tsx src/__tests__/Explorer.inventory.test.tsx src/components/store/__tests__/StoreComponentList.test.tsx src/components/store/__tests__/StoreComponentEditor.test.tsx src/components/store/__tests__/ImportComponentsDialog.test.tsx</automated>
  </verify>
  <done>The Wave 0 UI suites exist at the exact validation paths, encode the intended Phase 1 UI behavior including overwrite confirmation, and can be reused directly by Plans 01-02 and 01-03 as their automated verification.</done>
</task>

</tasks>

<verification>
Run the new `packages/ui` Vitest command against all four Phase 1 UI suites. Confirm the test runner is package-local and that each required validation file is executable by name.
</verification>

<success_criteria>
Phase 1 no longer relies on build-only verification for UI work: the UI package has its own test runner and the required source badge, Explorer, store list, and store editor suites are in place for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01-store-and-inventory-foundation/01-00-SUMMARY.md`
</output>
