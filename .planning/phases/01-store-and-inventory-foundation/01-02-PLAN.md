---
phase: 01-store-and-inventory-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-00
files_modified:
  - packages/ui/src/components/SourceBadge.tsx
  - packages/ui/src/hooks/usePlugins.ts
  - packages/ui/src/hooks/__tests__/usePlugins.test.tsx
  - packages/ui/src/Explorer.tsx
  - packages/cli/src/server/routes/__tests__/agents.test.ts
  - packages/cli/src/server/routes/__tests__/skills.test.ts
  - packages/cli/src/server/routes/__tests__/commands.test.ts
  - packages/cli/src/server/routes/__tests__/plugins.test.ts
  - packages/cli/src/server/routes/__tests__/configs.test.ts
autonomous: true
requirements:
  - INV-01
  - INV-02
must_haves:
  truths:
    - "User can tell whether an inventory item is local, profile-linked, or plugin-provided from the Explorer UI."
    - "User can open the Plugins section and inspect installed plugins, their enabled state, and bundled component counts."
    - "User can inspect current hooks, MCP servers, and LSP servers without entering an edit flow."
  artifacts:
    - path: "packages/ui/src/components/SourceBadge.tsx"
      provides: "Explicit source badges for local, profile, and plugin items"
    - path: "packages/ui/src/Explorer.tsx"
      provides: "Read-only environment inspection surfaces for inventory and plugins/configuration"
    - path: "packages/ui/src/hooks/usePlugins.ts"
      provides: "Normalized plugin inventory query consumed by Explorer"
  key_links:
    - from: "packages/ui/src/Explorer.tsx"
      to: "packages/ui/src/hooks/usePlugins.ts"
      via: "normalized plugin query result with stable enabled/count fields"
      pattern: "usePlugins\\("
    - from: "packages/ui/src/Explorer.tsx"
      to: "packages/ui/src/components/SourceBadge.tsx"
      via: "entity card badges"
      pattern: "SourceBadge source=\\{entity\\.source\\}"
    - from: "packages/cli/src/server/routes/agents.ts"
      to: "packages/ui/src/components/SourceBadge.tsx"
      via: "literal local/profile/plugin source values"
      pattern: "source: 'local'|'profile'|'plugin'"
---

<objective>
Build the read-only current-environment inspection experience for Phase 1.

Purpose: Make AGENT_HOME inventory and environment configuration legible without turning inventory into an editing surface.
Output: Explicit source badges, a populated Plugins view, and read-only Explorer sections for current configuration.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-store-and-inventory-foundation/01-CONTEXT.md
@.planning/phases/01-store-and-inventory-foundation/01-RESEARCH.md
@.planning/phases/01-store-and-inventory-foundation/01-UI-SPEC.md
@.planning/phases/01-store-and-inventory-foundation/01-00-PLAN.md
@packages/cli/src/server/routes/agents.ts
@packages/cli/src/server/routes/skills.ts
@packages/cli/src/server/routes/commands.ts
@packages/cli/src/server/routes/plugins.ts
@packages/ui/src/Explorer.tsx
@packages/ui/src/components/SourceBadge.tsx
@packages/ui/src/hooks/usePlugins.ts
@packages/ui/src/hooks/useConfigs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make inventory source labeling explicit and stable</name>
  <read_first>
    packages/ui/src/components/SourceBadge.tsx
    packages/ui/src/Explorer.tsx
    packages/cli/src/server/routes/agents.ts
    packages/cli/src/server/routes/skills.ts
    packages/cli/src/server/routes/commands.ts
    packages/cli/src/server/routes/__tests__/agents.test.ts
    packages/cli/src/server/routes/__tests__/skills.test.ts
    packages/cli/src/server/routes/__tests__/commands.test.ts
  </read_first>
  <files>
    packages/ui/src/components/SourceBadge.tsx
    packages/ui/src/Explorer.tsx
    packages/cli/src/server/routes/__tests__/agents.test.ts
    packages/cli/src/server/routes/__tests__/skills.test.ts
    packages/cli/src/server/routes/__tests__/commands.test.ts
  </files>
  <action>Update `SourceBadge` so it renders a visible chip for all three literal source values: `local`, `profile`, and `plugin`. The rendered text must stay exactly `local` and `profile`; plugin rows may include the plugin short name but must still visibly identify the row as plugin-provided. Use a neutral badge style for `local`, the existing blue-accent style for `profile`, and keep the purple plugin styling reserved for plugin items only. In `Explorer.tsx`, keep inventory views read-only and ensure every agent/skill/command card includes the badge output from `SourceBadge` with no source-specific omission. Extend the existing route tests so agents, skills, and commands still prove that regular filesystem items return `source: 'local'` and symlinked items return `source: 'profile'`.</action>
  <acceptance_criteria>
    `packages/ui/src/components/SourceBadge.tsx` contains explicit branches for `source === 'local'`, `source === 'profile'`, and `source === 'plugin'`.
    `packages/ui/src/components/SourceBadge.tsx` contains the literal text `local` and `profile`.
    `packages/ui/src/Explorer.tsx` still renders `SourceBadge source={entity.source}` in each entity card configuration.
    The agent, skill, and command route tests each contain assertions for both `source: local` and `source: profile`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @claudeui/cli test -- src/server/routes/__tests__/agents.test.ts src/server/routes/__tests__/skills.test.ts src/server/routes/__tests__/commands.test.ts && pnpm --filter @claudeui/ui test -- src/components/__tests__/SourceBadge.test.tsx</automated>
  </verify>
  <done>Explorer inventory rows always identify the origin of each item, and the backend source classification contract remains locked by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Explorer placeholders with read-only plugin and environment inspection panels</name>
  <read_first>
    packages/ui/src/Explorer.tsx
    packages/ui/src/hooks/usePlugins.ts
    packages/cli/src/server/routes/plugins.ts
    packages/cli/src/server/routes/__tests__/plugins.test.ts
    packages/cli/src/server/routes/__tests__/configs.test.ts
    packages/ui/src/hooks/__tests__/usePlugins.test.tsx
  </read_first>
  <files>
    packages/ui/src/Explorer.tsx
    packages/ui/src/hooks/usePlugins.ts
    packages/ui/src/hooks/__tests__/usePlugins.test.tsx
    packages/cli/src/server/routes/__tests__/plugins.test.ts
    packages/cli/src/server/routes/__tests__/configs.test.ts
  </files>
  <action>Replace the `plugins` placeholder in `Explorer.tsx` with a real read-only panel backed by `usePlugins()` and `useMarketplaces()`. Contain the known `enabledPlugins` shape mismatch from research at the UI hook boundary instead of widening Phase 1 into a settings-schema refactor: `usePlugins.ts` should normalize the real `/api/plugins` payload into a stable UI shape with a boolean `enabled`, `installs.length`, and zero-default component counts for `agents`, `skills`, and `commands`, even when component collections are absent or sparse. Add `packages/ui/src/hooks/__tests__/usePlugins.test.tsx` that mocks the actual route payload shape returned by the backend and proves the normalized hook output drives Explorer correctly for both enabled and disabled plugins. Keep hooks, MCP, and LSP sections read-only; do not introduce edit buttons or route changes from the Explorer. Add a small configuration summary block above or beside those sections that labels the current environment as `Current environment` and reports counts for hooks, MCP servers, and LSP servers derived from the existing `useHooks()`, `useMcpServers()`, and `useLspServers()` hooks. Leave `claude-md` out of scope unless a simple read-only summary already exists; do not add inline editors. Do not refactor `useConfigs.ts` or `ConfigSection.tsx` in this task unless blocked by implementation. Extend plugin/config route tests only where needed to lock the fields the UI reads, especially the route payload shape and empty-state responses.</action>
  <acceptance_criteria>
    `packages/ui/src/Explorer.tsx` no longer calls `renderPlaceholder()` for `activeSection === 'plugins'`.
    `packages/ui/src/Explorer.tsx` contains the exact label `Current environment`.
    `packages/ui/src/Explorer.tsx` references `usePlugins`, `useMarketplaces`, `useHooks`, `useMcpServers`, and `useLspServers`.
    `packages/ui/src/Explorer.tsx` renders plugin fields `id`, `enabled`, and component counts for `agents`, `skills`, and `commands`.
    `packages/ui/src/hooks/usePlugins.ts` contains an explicit normalization step that returns a stable boolean `enabled` and count fields the Explorer can read without depending on settings schema internals.
    `packages/ui/src/hooks/__tests__/usePlugins.test.tsx` proves the real `/api/plugins` response shape is normalized correctly for both enabled and disabled plugins.
    No new edit button labels such as `Edit`, `Save`, or `Update` are added to the Explorer plugin/config sections.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @claudeui/cli test -- src/server/routes/__tests__/plugins.test.ts src/server/routes/__tests__/configs.test.ts && pnpm --filter @claudeui/ui test -- src/hooks/__tests__/usePlugins.test.tsx src/__tests__/Explorer.inventory.test.tsx</automated>
  </verify>
  <done>The Explorer exposes installed plugins plus current hooks/MCP/LSP state as read-only inspection surfaces and preserves the Phase 1 inventory boundary.</done>
</task>

</tasks>

<verification>
Run the targeted CLI route tests and the named UI behavior suites for `SourceBadge` and `Explorer.inventory`. Confirm the Explorer no longer shows a placeholder for plugins and that inventory cards render source badges for local, profile, and plugin items.
</verification>

<success_criteria>
The current environment can be inspected without ambiguity: inventory rows clearly show source, installed plugins are visible with enabled state and bundled content counts, and hooks/MCP/LSP data is accessible from read-only Explorer sections.
</success_criteria>

<output>
After completion, create `.planning/phases/01-store-and-inventory-foundation/01-02-SUMMARY.md`
</output>
