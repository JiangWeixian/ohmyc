---
phase: 15.2-follow-impl-wireframe-design-in-claude-md-this-wireframe-is-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/components/active-profile-chip.tsx
  - packages/ui/src/components/command-palette-trigger.tsx
  - packages/ui/src/components/header.tsx
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
must_haves:
  truths:
    - "Every route will display the shared header once integrated in Plan 02"
    - "Active-profile chip shows current active profile name with avatar dot"
    - "Command palette pill is the sole visual trigger for opening the palette"
  artifacts:
    - path: "packages/ui/src/components/active-profile-chip.tsx"
      provides: "Active profile chip with dropdown"
      exports: ["ActiveProfileChip"]
    - path: "packages/ui/src/components/command-palette-trigger.tsx"
      provides: "⌘K pill trigger button"
      exports: ["CommandPaletteTrigger"]
    - path: "packages/ui/src/components/header.tsx"
      provides: "Shared header composing pill + chip"
      exports: ["Header"]
  key_links:
    - from: "Header"
      to: "ActiveProfileChip"
      via: "component composition"
    - from: "Header"
      to: "CommandPaletteTrigger"
      via: "component composition"
    - from: "ActiveProfileChip"
      to: "useProfiles"
      via: "hook import"
    - from: "CommandPaletteTrigger"
      to: "useCommandPalette"
      via: "hook import"
---

<objective>
Create the three header chrome components: ActiveProfileChip, CommandPaletteTrigger, and Header. These are the building blocks that will be mounted at the App level in Plan 02.

Purpose: Establish the shared header chrome per D-01/D-02 so all routes get identical header elements (active-profile chip + ⌘K pill) without each route rendering its own.
Output: Three new component files ready for import into App.tsx.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.2-follow-impl-wireframe-design-in-claude-md-this-wireframe-is-/15.2-CONTEXT.md
@packages/ui/src/hooks/use-profiles.ts
@packages/ui/src/components/ui/command-palette.tsx
@packages/ui/src/components/ui/dropdown-menu.tsx
@packages/ui/src/lib/utils.ts

<interfaces>
From use-profiles.ts:
```typescript
export function useProfiles(): {
  data?: { profiles: Profile[]; active: string | null }
  isLoading: boolean
}
```

From command-palette.tsx:
```typescript
export function useCommandPalette(): {
  open: () => void
  close: () => void
  isOpen: boolean
}
```

From dropdown-menu.tsx (existing shadcn component):
```typescript
export {
  DropdownMenu,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuSeparator,
} from "@/components/ui/dropdown-menu"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ActiveProfileChip component</name>
  <files>packages/ui/src/components/active-profile-chip.tsx</files>
  <action>
Create `ActiveProfileChip` component per D-03, D-04, D-05.

- Import `useProfiles` from `@/hooks/use-profiles`
- Import `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuSeparator` from `@/components/ui/dropdown-menu`
- Import `ChevronDown`, `User` from `lucide-react`
- Use `cn` from `@/lib/utils`

**Anatomy:**
- Container: `h-9 px-3 rounded-full border border-[rgba(255,255,255,0.08)] bg-[rgba(255,255,255,0.02)] flex items-center gap-2 cursor-pointer hover:bg-[rgba(255,255,255,0.04)] hover:border-[rgba(255,255,255,0.14)] transition-all duration-150`
- Avatar dot: 6px circle, `#f7f8f8` fill (only when active profile exists)
- Label: `12px weight 510`. "Active" prefix in `text-[#8a8f98]` (text-tertiary), profile name in `text-[#d0d6e0]` (text-secondary)
- Caret: `ChevronDown size={12} text-[#8a8f98]`

**Dropdown items (per D-04):**
1. Profile list: map over `profiles`, each clickable. On click: call `activateProfile` mutation (import `useActivateProfile` from `@/hooks/use-profiles`). Close dropdown after click.
2. Separator
3. "Compare…" — on click: navigate to `/profiles` and signal compare-open. For now, use `window.location.href = '/profiles?action=compare'` as a simple signal. Plan 03 will handle this query param.
4. "Manage profiles →" — navigate to `/profiles`

**Empty state (D-05):**
- If no active profile: show "No active profile" in `text-[#8a8f98]`, no avatar dot. Clicking still opens dropdown.

**Props interface:**
```typescript
interface ActiveProfileChipProps {
  onCompare?: () => void
  onNavigateToProfiles?: () => void
}
```

Note: The `onCompare` prop lets the parent (Header/App) decide how to trigger compare. If not provided, fallback to URL navigation.
  </action>
  <verify>
    <automated>cd packages/ui && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c "active-profile-chip" || echo "0"</automated>
  </verify>
  <done>Component file exists, compiles without errors, exports ActiveProfileChip with correct props interface</done>
</task>

<task type="auto">
  <name>Task 2: Create CommandPaletteTrigger component</name>
  <files>packages/ui/src/components/command-palette-trigger.tsx</files>
  <action>
Create `CommandPaletteTrigger` component per D-06, D-07, D-08.

- Import `useCommandPalette` from `@/components/ui/command-palette`
- Import `Search` from `lucide-react`
- Import `cn` from `@/lib/utils`

**Anatomy (D-08):**
- Container: `w-60 h-9 rounded-md border border-[rgba(255,255,255,0.08)] bg-[rgba(255,255,255,0.02)] flex items-center justify-between px-3 cursor-pointer hover:bg-[rgba(255,255,255,0.04)] hover:border-[rgba(255,255,255,0.14)] transition-all duration-150`
- Left: `Search size={14} text-[#8a8f98]` + "Search..." text in `text-[13px] text-[#8a8f98]`
- Right: `⌘K` keycap in `11px weight 510 text-[#62666d]` with `border border-[rgba(255,255,255,0.08)] rounded-sm px-1 py-0.5`

**Behavior:**
- Clicking opens the command palette via `useCommandPalette().open()`
- The existing global ⌘K keyboard listener in `CommandPaletteProvider` remains unchanged (D-07)
- This component is purely the visual trigger; it does NOT reimplement keyboard listening

**Props:** None needed. The component is self-contained.

**Important:** Do NOT replace the existing CommandPaletteProvider or CommandPalette dialog. This component only replaces the *trigger* mechanism. The actual palette dialog stays as-is.
  </action>
  <verify>
    <automated>cd packages/ui && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c "command-palette-trigger" || echo "0"</automated>
  </verify>
  <done>Component file exists, compiles without errors, exports CommandPaletteTrigger, clicking calls open() from context</done>
</task>

<task type="auto">
  <name>Task 3: Create Header component</name>
  <files>packages/ui/src/components/header.tsx</files>
  <action>
Create `Header` component per D-01, D-02.

- Import `ActiveProfileChip` from `./active-profile-chip`
- Import `CommandPaletteTrigger` from `./command-palette-trigger`
- Import `cn` from `@/lib/utils`

**Layout (right-to-left per D-02):**
- Outer: `h-16 bg-[#0f1011] border-b border-[rgba(255,255,255,0.05)] flex items-center justify-between px-6 shrink-0`
- Left side: empty placeholder div (for future notifications/secondary actions, per deferred ideas). Just render `<div className="flex-1" />` or leave empty.
- Right side: `flex items-center gap-3`
  - CommandPaletteTrigger
  - ActiveProfileChip

**Props interface:**
```typescript
interface HeaderProps {
  onCompare?: () => void
}
```

Pass `onCompare` through to ActiveProfileChip.

**Note:** The header is route-agnostic. It does not receive or render route-specific content. All routes get identical chrome.
  </action>
  <verify>
    <automated>cd packages/ui && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c "header.tsx" || echo "0"</automated>
  </verify>
  <done>Header component exists, compiles without errors, renders CommandPaletteTrigger left of ActiveProfileChip, no route-specific logic</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Header → useProfiles | Trusted internal hook |
| ActiveProfileChip → activate API | Calls existing mutation hook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-01 | Tampering | Compare… URL param | accept | Low risk — query param only triggers UI state, no data mutation |
| T-15.2-02 | Information Disclosure | Profile list in dropdown | accept | Profiles list is already public in sidebar; no new exposure |
</threat_model>

<verification>
- All three components compile: `cd packages/ui && npx tsc --noEmit --skipLibCheck`
- No runtime errors in dev build: `cd packages/ui && npm run dev` starts without error
</verification>

<success_criteria>
- ActiveProfileChip.tsx exports a working dropdown component with profile activation
- CommandPaletteTrigger.tsx exports a pill that opens the palette on click
- Header.tsx composes both and provides the shared chrome structure
- All files compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-follow-impl-wireframe-design-in-claude-md-this-wireframe-is-/15.2-01-SUMMARY.md`
</output>
