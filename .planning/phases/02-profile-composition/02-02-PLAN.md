---
phase: 02-profile-composition
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/ui/src/components/profiles/ProfileEditor.tsx
  - packages/ui/src/components/profiles/ComponentPicker.tsx
  - packages/ui/src/components/profiles/PluginPicker.tsx
  - packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx
  - packages/ui/src/__tests__/ProfilesView.test.tsx
autonomous: true
requirements:
  - PROF-01
  - PROF-02
must_haves:
  truths:
    - "User can create a named profile from one single-page editor without switching tabs or steps."
    - "User can edit description, component selections, plugins, hooks, MCP servers, LSP servers, and settings from grouped sections that match the approved UI contract."
    - "Invalid JSON blocks fail inline near the affected runtime-config section instead of only surfacing as a distant generic error."
  artifacts:
    - path: "packages/ui/src/components/profiles/ProfileEditor.tsx"
      provides: "Single-page profile editor grouped into Basics, Selections, and Runtime config"
    - path: "packages/ui/src/components/profiles/ComponentPicker.tsx"
      provides: "Selection groups for agents, skills, and commands"
    - path: "packages/ui/src/components/profiles/PluginPicker.tsx"
      provides: "Plugin selection group within the same editing flow"
    - path: "packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx"
      provides: "Behavior coverage for grouping, save payloads, and inline validation"
  key_links:
    - from: "packages/ui/src/components/profiles/ProfileEditor.tsx"
      to: "packages/ui/src/hooks/useProfiles.ts"
      via: "create/update mutations"
      pattern: "use(Create|Update)Profile\\("
    - from: "packages/ui/src/components/profiles/ProfileEditor.tsx"
      to: "packages/ui/src/components/profiles/ComponentPicker.tsx"
      via: "Selections section"
      pattern: "ComponentPicker"
    - from: "packages/ui/src/components/profiles/ProfileEditor.tsx"
      to: "packages/ui/src/components/profiles/PluginPicker.tsx"
      via: "Selections section"
      pattern: "PluginPicker"
---

<objective>
Restructure the profile editor into the approved composition workflow.

Purpose: Make profile creation and editing scanable and reliable without changing the existing single-page editing model.
Output: A sectioned editor with explicit `Basics`, `Selections`, and `Runtime config` groupings plus UI tests that lock save payloads and inline validation behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-profile-composition/02-CONTEXT.md
@.planning/phases/02-profile-composition/02-RESEARCH.md
@.planning/phases/02-profile-composition/02-UI-SPEC.md
@.planning/phases/02-profile-composition/02-VALIDATION.md
@packages/ui/src/components/profiles/ProfileEditor.tsx
@packages/ui/src/components/profiles/ComponentPicker.tsx
@packages/ui/src/components/profiles/PluginPicker.tsx
@packages/ui/src/hooks/useProfiles.ts
@packages/ui/src/hooks/useStore.ts
@packages/ui/src/hooks/usePlugins.ts
@packages/ui/src/components/JsonEditor.tsx
@packages/ui/src/test/renderWithProviders.tsx

<interfaces>
From `packages/ui/src/components/profiles/ProfileEditor.tsx`:

```tsx
interface ProfileEditorProps {
  profile?: Profile;
  onSaved: (name: string) => void;
  onCancel: () => void;
}
```

From `packages/ui/src/hooks/useProfiles.ts`:

```ts
export function useCreateProfile()
export function useUpdateProfile()
```

From `packages/ui/src/components/profiles/ComponentPicker.tsx`:

```tsx
interface ComponentPickerProps {
  label: string;
  available: { id: string; description?: string }[];
  selected: string[];
  onChange: (selected: string[]) => void;
  isLoading?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add editor behavior coverage before restructuring the form</name>
  <read_first>
    packages/ui/src/components/profiles/ProfileEditor.tsx
    packages/ui/src/__tests__/ProfilesView.test.tsx
    packages/ui/src/test/renderWithProviders.tsx
    .planning/phases/02-profile-composition/02-VALIDATION.md
    .planning/phases/02-profile-composition/02-UI-SPEC.md
  </read_first>
  <files>
    packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx
    packages/ui/src/__tests__/ProfilesView.test.tsx
  </files>
  <action>Create the missing Wave 0 UI coverage for the profile editor. Create `packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx` that renders both create and edit states using the existing `renderWithProviders` helper from `packages/ui/src/test/renderWithProviders.tsx`. The test file must:

1. Mock `@tanstack/react-query` hooks (`useCreateProfile`, `useUpdateProfile`) and store hooks (`useStoreAgents`, `useStoreSkills`, `useStoreCommands`) and plugin hooks (`usePlugins`) to return controlled data.
2. Assert the editor renders the exact section headings `Basics`, `Selections`, and `Runtime config` in order.
3. Assert the primary action button contains the text `Save Profile`.
4. Assert the `Selections` section contains grouped picker labels: `Agents`, `Skills`, `Commands`, and `Plugins`.
5. Assert that entering invalid JSON into a runtime config field (hooks, MCP, LSP, or settings) produces a visible error message near the affected field, not only as a distant global banner. Test this by simulating save with malformed JSON input and checking for error text.

Extend `packages/ui/src/__tests__/ProfilesView.test.tsx` only where needed so `/profiles/new` still routes to the editor surface and `/profiles/:name` can enter edit mode without breaking the existing store-route tests. Do not remove any existing test cases.</action>
  <acceptance_criteria>
    `packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx` exists and is a non-empty test file.
    `packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx` contains the exact string literals `Basics`, `Selections`, and `Runtime config` in assertions.
    `packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx` asserts that the primary save button text contains `Save Profile`.
    `packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx` contains a test case that verifies invalid JSON produces a visible error near the affected field.
    `packages/ui/src/__tests__/ProfilesView.test.tsx` still contains route coverage for `/profiles/agents`, `/profiles/skills`, and `/profiles/commands`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @claudeui/ui test -- src/components/profiles/__tests__/ProfileEditor.test.tsx src/__tests__/ProfilesView.test.tsx</automated>
  </verify>
  <done>The editor behavior contract exists before UI restructuring, and the missing Phase 2 test files are in place for create/edit flows.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the profile editor around the approved section model</name>
  <read_first>
    packages/ui/src/components/profiles/ProfileEditor.tsx
    packages/ui/src/components/profiles/ComponentPicker.tsx
    packages/ui/src/components/profiles/PluginPicker.tsx
    packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx
    packages/ui/src/components/JsonEditor.tsx
    .planning/phases/02-profile-composition/02-CONTEXT.md
    .planning/phases/02-profile-composition/02-UI-SPEC.md
    .claude/skills/linear-ui-skills/SKILL.md
  </read_first>
  <files>
    packages/ui/src/components/profiles/ProfileEditor.tsx
    packages/ui/src/components/profiles/ComponentPicker.tsx
    packages/ui/src/components/profiles/PluginPicker.tsx
    packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx
  </files>
  <action>Refactor `ProfileEditor.tsx` into one page with exactly three sections in this order: `Basics`, `Selections`, `Runtime config`. The component structure should be:

**Header** (existing pattern, keep pinned): Title (`Edit {name}` or `New Profile`), helper copy, and `Cancel` + `Save Profile` buttons. Change the primary button label from `Save` to `Save Profile`.

**Section: Basics** -- Wrap the existing name and description inputs in a section container. Add a section heading `Basics` and one short helper sentence such as `Name and description for this profile`. Keep the name input disabled during edit mode. Keep the description placeholder as-is.

**Section: Selections** -- Wrap the existing 2x2 grid of ComponentPickers (Agents, Skills, Commands) and PluginPicker in a section container. Add a section heading `Selections` and one short helper sentence such as `Choose store components and plugins to include in this profile`. The four pickers should render in this exact order: `Agents`, `Skills`, `Commands`, `Plugins`. Keep the existing grid layout (`grid gap-4 xl:grid-cols-2`).

**Section: Runtime config** -- Wrap the existing four JsonEditor blocks in a section container. Add a section heading `Runtime config` and one short helper sentence such as `Hooks, MCP servers, LSP servers, and settings overlay for this profile`. Each JsonEditor block must have:
- A visible label: `Hooks`, `MCP Servers`, `LSP Servers`, `Settings Overlay`
- One short helper sentence per block explaining what the block configures at a key-name level:
  - Hooks: `Define event hooks like preToolUse and postToolUse handlers.`
  - MCP Servers: `Configure Model Context Protocol servers by name.`
  - LSP Servers: `Configure Language Server Protocol servers by name.`
  - Settings Overlay: `Override Claude settings like model and effort level.`

**Error handling**: Replace the single global error banner with per-field inline errors for runtime JSON parse failures. Keep the global error region for mutation errors (server-side failures). The `handleSave` function should catch JSON parse errors per field and attach the error message to the corresponding field's state, not just to the global `error` state.

**Styling**: Use the Linear UI skill conventions -- `#080A0A` background, `#0F1112` for card surfaces, 16px spacing for section internals, 32px between sections, 6-8px border radius, 1px borders. Section headings use `text-[20px] font-semibold text-[var(--text-primary)]`. Helper text uses `text-[13px] text-[var(--text-secondary)]`.

Keep the flow single-page; do not introduce tabs, steppers, compare views, or activation diff previews. Preserve existing create/update mutations and payload shapes (`CreateProfileBody`, `UpdateProfileBody`). After implementation, run the test file created in Task 1 and update it if any text assertions need to match the actual rendered output.</action>
  <acceptance_criteria>
    `packages/ui/src/components/profiles/ProfileEditor.tsx` contains the exact section heading text `Basics` before `Selections` before `Runtime config` (in document order).
    `packages/ui/src/components/profiles/ProfileEditor.tsx` contains the exact button label `Save Profile`.
    `packages/ui/src/components/profiles/ProfileEditor.tsx` contains helper copy for hooks (`preToolUse` or `postToolUse`), MCP servers, LSP servers, and settings sections as visible text.
    `packages/ui/src/components/profiles/ProfileEditor.tsx` has per-field error state for runtime JSON fields instead of a single global error banner for parse failures.
    `packages/ui/src/components/profiles/ProfileEditor.tsx` preserves the `ProfileEditorProps` interface unchanged (`profile?: Profile; onSaved: (name: string) => void; onCancel: () => void;`).
    `packages/ui/src/components/profiles/ComponentPicker.tsx` and `PluginPicker.tsx` still export with the same props interfaces.
    `packages/ui/src/components/profiles/__tests__/ProfileEditor.test.tsx` passes after the restructuring.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @claudeui/ui test -- src/components/profiles/__tests__/ProfileEditor.test.tsx src/__tests__/ProfilesView.test.tsx</automated>
  </verify>
  <done>The profile editor matches the approved single-page composition structure and supports clear create/edit flows for every Phase 2 profile field.</done>
</task>

</tasks>

<verification>
Run the new editor suite and the existing `ProfilesView` route suite. Confirm the editor exposes the exact three required sections, the four selection groups, and inline runtime-config validation.
</verification>

<success_criteria>
Users can create and edit profiles through one scanable page that matches the approved composition structure and covers every required profile field without tabs or raw error ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-composition/02-02-SUMMARY.md`
</output>
