---
phase: 02-profile-composition
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - packages/ui/src/ProfilesView.tsx
  - packages/ui/src/hooks/useProfiles.ts
  - packages/ui/src/components/profiles/ProfileCard.tsx
  - packages/ui/src/components/profiles/ProfilesSidebar.tsx
  - packages/ui/src/components/profiles/__tests__/ProfileCard.test.tsx
  - packages/ui/src/__tests__/ProfilesView.test.tsx
autonomous: false
requirements:
  - PROF-03
  - PROF-04
must_haves:
  truths:
    - "User can inspect which agents, skills, commands, plugins, and runtime-config keys belong to a profile before activation."
    - "User can tell which profile is active from the sidebar, detail view, and activation feedback without seeing a filesystem path as the primary signal."
    - "Profile detail reads as an activation-readiness view, not as a count-only summary card or raw JSON dump."
  artifacts:
    - path: "packages/ui/src/components/profiles/ProfileCard.tsx"
      provides: "Two-column activation-readiness detail view"
    - path: "packages/ui/src/components/profiles/ProfilesSidebar.tsx"
      provides: "Consistent active-name treatment in the profile list"
    - path: "packages/ui/src/ProfilesView.tsx"
      provides: "Activation/deactivation feedback and profile-detail routing"
    - path: "packages/ui/src/components/profiles/__tests__/ProfileCard.test.tsx"
      provides: "Coverage for component lists, runtime summaries, and active badge rendering"
  key_links:
    - from: "packages/ui/src/ProfilesView.tsx"
      to: "packages/ui/src/hooks/useProfiles.ts"
      via: "active profile query and activation/deactivation mutations"
      pattern: "useProfiles\\("
    - from: "packages/ui/src/ProfilesView.tsx"
      to: "packages/ui/src/components/profiles/ProfileCard.tsx"
      via: "selected profile detail rendering"
      pattern: "ProfileCard"
    - from: "packages/ui/src/components/profiles/ProfilesSidebar.tsx"
      to: "packages/ui/src/hooks/useProfiles.ts"
      via: "active-name comparison for list badge state"
      pattern: "active"
---

<objective>
Finish the user-facing Phase 2 profile inspection experience.

Purpose: Turn profile detail and active-state surfaces into a clear pre-activation view driven by the normalized backend contract.
Output: A two-column profile detail layout, consistent active-profile badges and feedback, route-level UI coverage, and a final human verification checkpoint for scanability.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-profile-composition/02-CONTEXT.md
@.planning/phases/02-profile-composition/02-RESEARCH.md
@.planning/phases/02-profile-composition/02-UI-SPEC.md
@.planning/phases/02-profile-composition/02-VALIDATION.md
@packages/ui/src/ProfilesView.tsx
@packages/ui/src/hooks/useProfiles.ts
@packages/ui/src/components/profiles/ProfileCard.tsx
@packages/ui/src/components/profiles/ProfilesSidebar.tsx
@packages/ui/src/__tests__/ProfilesView.test.tsx
@packages/ui/src/test/renderWithProviders.tsx

<interfaces>
From `packages/ui/src/hooks/useProfiles.ts`:

```ts
interface ProfilesListResponse {
  profiles: Profile[];
  active: string | null;  // Now returns profile name (not path) after Plan 02-01
}
```

From `packages/ui/src/components/profiles/ProfileCard.tsx`:

```tsx
interface ProfileCardProps {
  profile: Profile;
  isActive: boolean;
  onActivate: () => void;
  onDeactivate: () => void;
  onDelete: () => void;
  onEdit: () => void;
}
```

From `packages/shared/src/profileSchema.ts`:

```ts
export type Profile = {
  name: string;
  description?: string;
  agents: string[];
  skills: string[];
  commands: string[];
  plugins: string[];
  hooks?: any;
  mcpServers?: any;
  lspServers?: any;
  settings?: Record<string, any>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the count-first profile card with an activation-readiness detail view</name>
  <read_first>
    packages/ui/src/components/profiles/ProfileCard.tsx
    packages/ui/src/ProfilesView.tsx
    packages/ui/src/components/profiles/ProfilesSidebar.tsx
    packages/ui/src/hooks/useProfiles.ts
    packages/ui/src/__tests__/ProfilesView.test.tsx
    .planning/phases/02-profile-composition/02-CONTEXT.md
    .planning/phases/02-profile-composition/02-UI-SPEC.md
    .claude/skills/linear-ui-skills/SKILL.md
  </read_first>
  <files>
    packages/ui/src/ProfilesView.tsx
    packages/ui/src/components/profiles/ProfileCard.tsx
    packages/ui/src/components/profiles/ProfilesSidebar.tsx
    packages/ui/src/components/profiles/__tests__/ProfileCard.test.tsx
    packages/ui/src/__tests__/ProfilesView.test.tsx
  </files>
  <action>This task has three coordinated changes across files:

**1. ProfilesView.tsx -- Use exact name comparison for active state**

At line 149, replace the path-inference check:
```tsx
// BEFORE:
isActive={active?.includes(selectedProfile.name) ?? false}
// AFTER:
isActive={active === selectedProfile.name}
```

The `active` value now comes from the normalized backend (Plan 02-01) as a profile name string, not a path. No `.includes()` heuristic needed.

Update activation toast copy to name the profile directly without quoting:
```tsx
// In handleActivate onSuccess:
success(`Activated ${name}`);
// In handleDeactivate onSuccess:
success(`Deactivated ${name}`);
```

Keep filesystem paths out of primary toast copy. The `result.warnings` should still be surfaced if present, but as secondary information.

**2. ProfileCard.tsx -- Two-column activation-readiness layout**

Replace the current implementation entirely. The new `ProfileCard` must render:

Left column (summary rail, ~35% width on desktop):
- Profile name in display size (`text-[30px] font-semibold text-[var(--text-primary)]`)
- `Active` text badge next to the name when `isActive` is true. The badge uses accent color (`bg-[#5E6AD2]/15 text-[#5E6AD2] text-[11px] font-medium px-2 py-0.5 rounded`). Do NOT use the `Star` icon as the sole active indicator; use the text badge `Active`.
- Description below the name in secondary text
- Action buttons row: `Edit`, `Activate`/`Deactivate`, `Delete` (keep existing styling patterns)
- Optional muted technical metadata below actions (do NOT show generated profile path as primary content)

Right column (~65% width on desktop), exactly two stacked sections:

Section: `Components`
- Heading: `Components` in `text-[20px] font-semibold`
- Helper: `Store-managed items included in this profile`
- Four sub-groups in order: `Agents`, `Skills`, `Commands`, `Plugins`
- Each sub-group shows a small label and lists the included component names as readable text
- Empty groups show `No {group} selected` (e.g., `No agents selected`)
- Do NOT show raw component metadata; names are primary, descriptions are secondary muted text

Section: `Runtime config`
- Heading: `Runtime config` in `text-[20px] font-semibold`
- Helper: `Configuration blocks applied when this profile is activated`
- Four sub-groups in order: `Hooks`, `MCP`, `LSP`, `Settings`
- Each sub-group lists the top-level keys from the profile's runtime config:
  ```tsx
  // Derived from profile data:
  const hooksKeys = profile.hooks ? Object.keys(profile.hooks) : [];
  const mcpKeys = profile.mcpServers ? Object.keys(profile.mcpServers) : [];
  const lspKeys = profile.lspServers ? Object.keys(profile.lspServers) : [];
  const settingsKeys = profile.settings ? Object.keys(profile.settings) : [];
  ```
- Render keys as compact chips or stacked key rows with monospace font for the key name itself
- Empty groups show `No hooks configured`, `No MCP servers configured`, `No LSP servers configured`, `No settings configured`
- Do NOT render raw JSON (`<pre>`) as the default presentation. Remove the existing `<pre>` block for `profile.settings`.

Layout collapses to single column below desktop breakpoint (summary rail first, then sections below).

**3. ProfilesSidebar.tsx -- Replace star-only indicator with Active badge**

At line 41, replace the path-inference check:
```tsx
// BEFORE:
const isActive = active?.includes(p.name);
// AFTER:
const isActive = active === p.name;
```

Replace the `Star` icon (line 62-68) with a text badge matching the detail view treatment:
```tsx
{isActive && (
  <span className="shrink-0 text-[10px] font-medium text-[#5E6AD2] bg-[#5E6AD2]/15 px-1.5 py-0.5 rounded">
    Active
  </span>
)}
```

Remove the `Star` import from lucide-react if no longer used.

**4. ProfileCard.test.tsx -- Create coverage**

Create `packages/ui/src/components/profiles/__tests__/ProfileCard.test.tsx` that:
1. Renders a profile with populated agents/skills/commands/plugins and asserts the component names appear in the document
2. Renders a profile with hooks/mcpServers/lspServers/settings and asserts the top-level key names appear
3. Renders an active profile and asserts the `Active` badge text is visible
4. Renders a profile with empty groups and asserts empty-state strings appear (`No agents selected`, `No hooks configured`, etc.)
5. Asserts no `<pre>` element renders raw JSON for settings

**5. ProfilesView.test.tsx -- Extend active-state coverage**

Extend the existing test file to cover:
- `/profiles/{name}` renders the detail view with the profile name visible
- Active badge rendering when the active profile matches
- Activation feedback names the profile directly (toast text contains `Activated`)

Do not break existing route tests.</action>
  <acceptance_criteria>
    `packages/ui/src/ProfilesView.tsx` no longer contains `.includes(` for active profile determination; uses `=== ` instead.
    `packages/ui/src/ProfilesView.tsx` contains toast text `Activated ${name}` (not `Profile "${name}" activated`).
    `packages/ui/src/components/profiles/ProfileCard.tsx` contains the exact section heading text `Components` and `Runtime config`.
    `packages/ui/src/components/profiles/ProfileCard.tsx` contains empty-state strings: `No agents selected`, `No skills selected`, `No commands selected`, `No plugins selected`, `No hooks configured`, `No MCP servers configured`, `No LSP servers configured`, `No settings configured`.
    `packages/ui/src/components/profiles/ProfileCard.tsx` does NOT contain `<pre>` with raw settings JSON as the primary detail surface.
    `packages/ui/src/components/profiles/ProfileCard.tsx` contains the text `Active` as a visible badge (not just a `Star` icon).
    `packages/ui/src/components/profiles/ProfilesSidebar.tsx` no longer uses `.includes(` for active comparison.
    `packages/ui/src/components/profiles/ProfilesSidebar.tsx` no longer imports `Star` from lucide-react (or `Star` is unused).
    `packages/ui/src/components/profiles/ProfilesSidebar.tsx` contains the text `Active` as a badge instead of a star icon.
    `packages/ui/src/components/profiles/__tests__/ProfileCard.test.tsx` exists and asserts active badge, component names, runtime key names, and empty states.
    `packages/ui/src/__tests__/ProfilesView.test.tsx` contains a test case asserting activation toast feedback.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @claudeui/ui test -- src/components/profiles/__tests__/ProfileCard.test.tsx src/__tests__/ProfilesView.test.tsx</automated>
  </verify>
  <done>Profile detail becomes a readable pre-activation inspection surface, and active-state presentation is consistent across list, detail, and feedback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the profile detail and editor scanability in the live UI</name>
  <read_first>
    packages/ui/src/components/profiles/ProfileCard.tsx
    packages/ui/src/components/profiles/ProfileEditor.tsx
    .planning/phases/02-profile-composition/02-UI-SPEC.md
    .planning/phases/02-profile-composition/02-VALIDATION.md
  </read_first>
  <files>
    packages/ui/src/components/profiles/ProfileCard.tsx
    packages/ui/src/components/profiles/ProfileEditor.tsx
    packages/ui/src/ProfilesView.tsx
  </files>
  <action>After Task 1 passes its automated suite, run the targeted UI tests again to confirm the latest build is stable, then perform a blocking human scanability check against the approved Phase 2 contract. Do not change implementation during this checkpoint unless the human reports specific issues.</action>
  <acceptance_criteria>
    The summary rail visibly leads with profile name and `Active` badge.
    The right column reads as exactly two sections: `Components` and `Runtime config`.
    The editor reads as one single-page flow with `Basics`, `Selections`, and `Runtime config` in order.
    No primary copy on the detail view or activation feedback depends on showing a filesystem path.
  </acceptance_criteria>
  <what-built>Profile detail now exposes activation-readiness information by component and runtime-config keys, and the editor is grouped into the approved single-page structure. Active-state presentation uses name-based `Active` text badges consistently across sidebar, detail, and activation feedback.</what-built>
  <how-to-verify>
    1. Start the app and open a populated profile at `/profiles/{name}`.
    2. Confirm the page anchors on the profile name with a visible `Active` badge when that profile is active.
    3. Confirm the detail layout reads as a summary rail plus `Components` and `Runtime config`, with names or key chips instead of raw JSON.
    4. Enter edit mode or open `/profiles/new` and confirm the page reads top-to-bottom as `Basics`, `Selections`, then `Runtime config`.
    5. Activate a profile and confirm the success feedback names the profile directly, e.g. `Activated daily`.
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @claudeui/ui test -- src/components/profiles/__tests__/ProfileCard.test.tsx src/components/profiles/__tests__/ProfileEditor.test.tsx src/__tests__/ProfilesView.test.tsx</automated>
  </verify>
  <resume-signal>Type `approved` if the layout and copy are clear, or describe the specific UI issues to fix.</resume-signal>
  <done>Human verification confirms the Phase 2 profile UI is scanable and consistent with the approved layout and copy contract.</done>
</task>

</tasks>

<verification>
Run the new `ProfileCard` and `ProfilesView` suites before asking for human verification. The automated pass must prove active-name UI logic, component/runtime summaries, and route rendering behavior.
</verification>

<success_criteria>
Users can inspect profile contents before activation, understand which profile is active everywhere in the UI, and the live interface passes a human scanability check against the approved Phase 2 UI contract.
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-composition/02-03-SUMMARY.md`
</output>
