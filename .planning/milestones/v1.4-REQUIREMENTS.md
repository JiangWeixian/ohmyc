# Requirements: v1.4 Project-Aware Loading + .cu Rebrand

**Defined:** 2026-04-17
**Shipped:** 2026-04-19
**Status:** Complete

## Foundation

- [x] **FOUND-01**: Server discovers project-local `.claude/` directory via CWD check at startup — Phase 10
- [x] **FOUND-02**: All path resolution centralized through ConfigLocator — no scattered `.claude`/`.cui` string literals — Phase 10
- [x] **FOUND-03**: SourceBadge UI renders `project` variant for project-scoped inventory items — Phase 10

## Project Loading

- [x] **LOAD-01**: Agents, skills, commands, and configs routes return merged results from global and project directories — Phase 11
- [x] **LOAD-02**: Project-scoped items override global items when both provide the same component name — Phase 11
- [x] **LOAD-03**: All merged items tagged with `source` field (`global`/`project`) in API responses — Phase 11

## .cu Rebrand

- [x] **REBR-01**: CLI writes all managed data (store, profiles, activation output) to `~/.cui/` instead of `~/.claude/` — Phase 12
- [x] **REBR-02**: Existing `~/.claude/` data remains readable after rebrand so users don't lose existing configurations — Phase 12

## Deferred to v1.5+

- **SETP-01**: Per-key settings provenance display showing which layer contributed each settings key
- **SETP-02**: Visual merge conflict indicators — both items shown with source badges, project wins
- **MIGR-01**: Guided migration helper — one-time guided move from `.claude` to `.cui` for ClaudeUI data only
- **CLI-01**: CLI auto-update mechanism
- **CLI-02**: CLI configuration file for default port, browser preference, etc.
- **CLI-03**: `cu dev` mode for development with hot reload
